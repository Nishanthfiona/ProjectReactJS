import React from "react";
import ReactDOM from "react-dom/client";
import EmojiMind from "./EmojiMind";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <EmojiMind />
  </React.StrictMode>
);